设计逻辑：在低频数据中对高频数据做处理
根据功能，设计相应的类，析构函数中，设置接受和发送的主题
为什么要另外设计IMU高频里程计发布类：
  因为IMU里程计误差较大，这里仅仅使用增量来计算真实位姿
比如类IMUPreintegration，需要实现计算预积分和优化imu参数的功能，这里需要接受imu数据（用于计算）+激光里程计（用于优化）
  在构造函数中，根据先验噪声模型和IMU偏执，初始化gtsam的预积分量
  IMU回调，将数据push入队列，如果有最新的激光里程计数据，
  
  
数据流
IMU -> IMU回调 -> 根据激光里程计输出高频IMU里程计
激光里程计 -> 回调 -> 计算两个激光里程计之间的预积分 -> 优化处新的激光里程计和预积分量
